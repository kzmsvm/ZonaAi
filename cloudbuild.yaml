steps:
  - name: 'gcr.io/cloud-builders/gcloud'
    args:
      - run
      - deploy
      - zona-api
      - --source=.
      - --region=us-central1
      - --platform=managed
      - --allow-unauthenticated
